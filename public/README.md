# Static assets

Place the listings-hero background image here as `hero-background.jpg`
(referenced by `.listings-hero` in `src/app/globals.css`). The hero renders
fine without it — the background falls back to a plain gradient — so this
is purely cosmetic.
